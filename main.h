#ifndef MAIN_H
#define MAIN_H
#include <unistd.h>
#include <stdarg.h>
#include <stddef.h>
#include <stdio.h>

int _printf(const char *format, ...);
int _putchar(char c);
int _isdigit(int c);
int put_str(char *str);

#endif
